{
  "name": "gcl-application",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && (git remote remove origin 2>/dev/null; true) && git remote add origin https://$GH_TOKEN@github.com/lanalabelle04/gcl-application.git && git add -A && git commit -m 'deploy' && git -c credential.helper='' push -f origin HEAD:gh-pages && cd .."
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@vitejs/plugin-react": "^4.3.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.2.2",
    "vite": "^5.4.2"
  }
}
